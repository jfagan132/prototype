<%= form_for(@reccomendation) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :opinion, placeholder: "Write you opinion here" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>